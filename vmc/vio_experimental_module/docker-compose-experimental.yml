version: "3"

services:

  vio-expiremental:
    image: vio-experimental:v1
    privileged: true
    restart: on-failure

  # sandbox:
  #   depends_on:
  #     - mqtt
  #   build: vrc_sandbox
  #   restart: on-failure
